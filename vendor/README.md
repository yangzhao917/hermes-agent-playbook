# Vendor Assets

This directory stores external or upstream skills that AgentOS depends on but does not treat as core product modules.

Vendor assets may be useful for runtime capability, integration reference, or fallback workflows. They should not be confused with AgentOS-owned product modules.

Current vendor assets:

- `humanize-zh`
- `mmx-cli`

已删除不可用 vendor：`agent-reach`, `just-one-api`。
