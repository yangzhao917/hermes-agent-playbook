# WeChat Chat Integration

Current status: high-risk planned integration.

WeChat chat records are sensitive and noisy. The product rule should be:

```text
summarize first, ask for confirmation, then persist selected facts
```

Do not ingest everything into long-term memory by default.
